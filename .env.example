# Neon Postgres connection string (pooled). Required for the Prisma client and CLI.
# Get this from the Neon console: Project → Connect → copy the "Prisma" connection string.
DATABASE_URL="postgresql://<user>:<password>@<host>/<db>?sslmode=require"
