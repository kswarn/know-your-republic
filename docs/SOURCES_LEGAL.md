# Source terms of use & licensing

Checked 2026-07-21, ahead of writing the first adapter. This is a working legal note,
not a substitute for counsel — flag anything below marked **needs review** before
scaling ingestion past the Phase 0 proof-of-pipeline adapter.

## Method

For each source: read `robots.txt` with a normal browser user agent, look for a
published terms-of-use / copyright page, and note whether the site's own
infrastructure blocks simple automated requests regardless of what `robots.txt` says
(a WAF or bot-detection layer is a practical constraint even where the policy text
would allow scraping).

## Per-source findings

| Source | robots.txt | Practical access | Licensing note |
|---|---|---|---|
| **India Code** (indiacode.nic.in) | Allows crawling except `/discover`, `/simple-search`, `/statistics`, `/contact`, `/feedback`, `/forgot`, `/login`, `/register` — item pages (`/handle/...`) are **not** disallowed. | Homepage responds to a plain HTTP client; **item/detail pages currently return 403 or time out** for both this tool's fetcher and a bare `curl` with a browser user-agent. Reads as bot-detection (WAF/JS challenge) on deep pages rather than a stated prohibition. | Statutes are government works; the site does not publish a separate restrictive license for the text itself. **Needs review**: confirm the reprint/redistribution terms on the site's own "About/Copyright" page once a page load succeeds, and budget engineering time for a headless-browser or session-aware fetch before Phase 2 bulk ingestion. |
| **Digital Sansad** (sansad.in) | No `robots.txt` found (404) — no crawl restriction stated. | Not yet load-tested against detail pages (MP profiles, bill trackers). | **Needs review** before first ingestion run — check for a terms/API page; sansad.in is the primary source for MP profiles and bill status, so this blocks Phase 1/2 people + laws ingestion, not Phase 0. |
| **PRS Legislative Research** (prsindia.org) | Reachable; redirects present, no disallow observed for content pages. | Secondary source only per the build plan — used to cross-check, never as the sole citation. | Confirm attribution requirements if any PRS summary text is quoted rather than paraphrased. |
| **MyNeta / ADR** (myneta.info) | `robots.txt` present (200), no blanket disallow observed. | Secondary/cross-check source only, per the build plan. | Standard affidavit-data reuse; attribute as "ADR/MyNeta" per their stated convention (confirm exact wording when first used). |
| **data.gov.in** (OGD Platform India) | Reachable via redirect; has a published API. | Prefer the official API over scraping HTML — this is the one source in the table with a documented, sanctioned bulk-access path. | **GODL-India** (Government Open Data License – India) applies. Attribution text to carry wherever OGD data is displayed: *"Data sourced from the Open Government Data (OGD) Platform India, data.gov.in, under the Government Open Data License – India (GODL-India)."* |
| **PIB** (pib.gov.in) | Reachable; redirect present, no disallow observed for release pages. | Used only to confirm current office-holders — low request volume, no bulk scraping planned. | Government press releases; no separate restrictive license expected. |
| **Election Commission of India** (eci.gov.in) | Returned 403 to a plain request. | **Needs review** — bot-detection observed even at the root; will need the same treatment as India Code before the Phase 3 locator work (constituency GeoJSON, current members) begins. | GODL-India likely applies to ECI open datasets specifically (as opposed to the general eci.gov.in site) — confirm before ingesting delimitation GeoJSON. |
| **Supreme Court of India** (sci.gov.in) | `robots.txt` present (200), no blanket disallow observed. | Judges + biography pages only, per the guardrail that judiciary content never includes case data. | No separate license stated; treat as a standard government work. |
| **High Courts / Dept. of Justice** (doj.gov.in and per-HC sites) | Checked for Karnataka HC specifically (2026-07-22), see below. The remaining 24 not yet checked individually. | **Needs review per state**, rolled out alongside the state-by-state ingestion plan in Phase 1/2, not all at once. | Check each HC site's own terms before its adapter ships. |
| **Karnataka High Court** (karnatakajudiciary.kar.nic.in / judiciary.karnataka.gov.in) | The old domain doesn't resolve at all (`curl`: connection refused, not a WAF response). The new domain's judges page loads (200) but is a JS-rendered SPA shell — `curl` returns an empty app shell with no judge data in the raw HTML. | **Blocked.** Attempted the Karnataka HC's own site (unreachable) and the Department of Justice's per-court page `doj.gov.in/karnataka-high-court-5` (reachable for a plain page, but Akamai-blocks both `curl` and headless Chromium with `Access Denied`, same edge-block pattern as india.gov.in's Who's Who and legislative.gov.in's regional-language pages). No primary source could be reached for the current judge roster. The only source actually reachable was Wikipedia's "List of sitting judges of the high courts of India" (Karnataka section) — a tertiary aggregator, not the Court's own record, so it was **not** used as a stand-in for a primary citation; Karnataka HC judges are not yet ingested pending a reachable official source (or a manually supplied one). | **Needs review** once reachable. |
| **State portals** (e.g. karnataka.gov.in, kla.kar.nic.in) | Not yet checked. | **Needs review**, first for Karnataka per the plan's "prove the pattern on one state first" approach. | Check before the first state adapter (Phase 1/2). |
| **Legislative Dept.** (lddashboard.legislative.gov.in) | Not checked — direct connections to the host timed out (see note). | Every direct connection attempt (this tool's fetcher, and `curl` with a browser user-agent) **timed out** rather than returning any HTTP response — distinct from India Code's 403/WAF pattern, this looks like the host is unreachable from this environment's network path. The real page (`https://lddashboard.legislative.gov.in/constitution-of-india`) was confirmed via search-engine indexing as the Legislative Department's official Constitution of India page. | **Needs review** once reachable. Constitution text is a government work; no separate restrictive license expected. Fundamental Rights content (Phase 1) cites this URL as a manually verified record, same pattern as the Phase 0 India Code seed — the URL's correctness was confirmed via search, not a live page load. |
| **Legislative Dept.** (`www.legislative.gov.in`, distinct host from `lddashboard.` above) | Not checked separately. | Unlike `lddashboard.`, direct PDF URLs under `www.legislative.gov.in/static/uploads/...` **do resolve** (`curl` HEAD returns 200, `content-type: application/pdf`, plausible file size) even though the human-facing pages (`/constitution-of-india-in-hindi`, `/constitution-of-india-in-kannada`) are Akamai-blocked for both plain `curl` and headless Chromium (`Access Denied`, edgesuite.net reference). Used for the Laws page's Constitution-of-India PDF table (2026-07-22): English (`.../2025/07/359f70a69695affb9d72f8393102bd2e.pdf`, "As on 11th November, 2025") and Hindi (`.../2025/07/ca7ce5c746fa7480804bbdeb6cb704f0.pdf`) were confirmed this way — URL correctness cross-checked via search-engine indexing (page title matched the expected language), then the file itself HEAD-verified as a live, correctly-typed PDF. **Kannada was not added**: no official `legislative.gov.in`-hosted PDF could be located (search kept resurfacing the English PDF as a false match); the only Kannada copies found were a private NGO mirror (makkalahakku.com, undated but pre-2011-amendment scope) and an Internet Archive user upload with no stated publisher — neither meets this project's "official primary source" bar, so the table ships with English + Hindi only until an official Kannada link is found or supplied. | Government work; no separate restrictive license expected once a page/PDF is confirmed reachable. |
| **National Portal Who's Who** (india.gov.in/directory/whos-who) | Not checked directly. | Blocked by an Akamai edge WAF for both a plain `curl` and a real headless-Chromium request (`Access Denied`, reference-numbered edge error) — a harder block than any other source hit so far, seemingly targeting automated/headless traffic specifically rather than a missing/odd `robots.txt`. | **Needs review.** This is the designated primary source for confirming state Chief Ministers (build plan §5, "Council of Ministers, portfolios"). Blocked access means state CM ingestion must cite each state's own CMO/government portal instead, verified one state at a time — starting with Karnataka per the plan's own "prove the pattern on one state first" approach. |
| **INDIAN-SHAPEFILES** (github.com/datta07/INDIAN-SHAPEFILES) | Not applicable (GitHub, not scraped). | Downloaded once via `raw.githubusercontent.com`; not re-fetched at runtime. | **MIT licensed** — vendored at `public/geo/india-states.geojson` (properties trimmed to `STNAME`/`STNAME_SH`; geometry untouched). This is community-compiled boundary data, not a direct Survey-of-India/ECI export — accurate enough for a state/UT-level outline map, but should be swapped for an ECI/Survey-of-India source before any feature that needs constituency- or ward-level precision (Phase 3). The dataset predates the January 2020 merger of Daman & Diu with Dadra & Nagar Haveli — both legacy shapes are kept and mapped to the single, current `Jurisdiction` record in code, rather than editing the geometry. |

## What this means for the adapter contract

- Every adapter in `src/lib/sources/` must be written to **fail closed**: if a fetch
  returns anything other than a clean 200 with the expected shape, the adapter must
  throw rather than insert a partial or guessed record. A blocked request is not a
  "no data" signal to paper over.
- Adapters must identify themselves with a descriptive `User-Agent` (not a browser
  impersonation string) so that any source operator can identify and rate-limit this
  project's traffic rather than mistake it for anonymous scraping traffic.
- Where a site's practical access is currently blocked (India Code detail pages, ECI
  root), Phase 0/1 seeding may use a manually verified record (URL and content
  confirmed correct by a human, via search-engine cache or direct browser check) while
  the automated-fetch path is built out — the `Citation.sourceUrl` must still resolve
  to the real, correct official page.
- Re-review this note before Phase 2's bulk state-by-state ingestion, and again before
  Phase 4's scheduled auto-currency polling — both meaningfully increase request
  volume against sources marked "needs review" above.
